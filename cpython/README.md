Simple experiments with CPython Modules.

# How to use

You will need gcc, Python 3 and the python3-dev package.

Create a virtualenv to install the CPython modules.

Compile and install them:

python setup.py install
